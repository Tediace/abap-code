@EndUserText.label : 'map the fiscal correction category and formula correction'
@AbapCatalog.enhancementCategory : #NOT_EXTENSIBLE
@AbapCatalog.tableCategory : #TRANSPARENT
@AbapCatalog.deliveryClass : #A
@AbapCatalog.dataMaintenance : #ALLOWED
define table zftax_cit_map {
  key client           : abap.clnt not null;
  key gl_account       : abap.char(10) not null;
  permanent_difference : abap_boolean;
  timing_difference    : abap_boolean;
  formula              : zde_cit_formula;

}